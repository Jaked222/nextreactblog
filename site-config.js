export const colors = {
  darkMode: {
    primaryText: {
      varName: "--primary-text",
      color: "white",
    },
    secondaryText: {
      varName: "--secondary-text",
      color: "hsla(0, 0%, 100%, 0.6)",
    },
    callToAction: {
      varName: "--call-to-action",
      color: "goldenrod",
    },
    containerBackground: {
      varName: "--container-background",
      color: "#141317",
    },
    postBackground: {
      varName: "--post-background",
      color: "#23212a",
    },
  },
  lightMode: {
    primaryText: {
      varName: "--primary-text",
      color: "#000000",
    },
    secondaryText: {
      varName: "--secondary-text",
      color: "#6D6A6F",
    },
    callToAction: {
      varName: "--call-to-action",
      color: "#7C20D8",
    },
    containerBackground: {
      varName: "--container-background",
      color: "white",
    },
    postBackground: {
      varName: "--post-background",
      color: "#F5F5F5",
    },
  },
};

export const quotes = [
  "Do all things one at a time and fully",
  "When it comes to practice, see nothing that happens as good or bad",
  "Sometimes it's better to travel than to arrive",
  "Technology is the offspring of science. Science is the offspring of rationality.",
];

export const displayQuoteSection = true;
export const cardEnabled = true;
